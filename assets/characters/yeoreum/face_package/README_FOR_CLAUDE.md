# Claude note — Face Package v1.1

This package is **not** a production reference-order update.

For the current locked Natural/Mirror lane, continue to use:

```text
FACE_ID_MASTER + CHARACTER_MASTER + BODY_MASTER + FACE-MASKED DAILY_REFERENCE
```

Only `YR_FACE_00_ID_MASTER` is currently production-eligible for the face slot.

The other approved library images are for QC/viewpoint-expression coverage and future explicit A/B testing. Do not automatically replace the Face ID Master and do not load the entire package into Seedream.

See `FACE_PACKAGE_MANIFEST.md` for roles and status.
