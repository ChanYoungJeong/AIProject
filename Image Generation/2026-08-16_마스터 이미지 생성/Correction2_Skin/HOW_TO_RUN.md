# Correction2_Skin - 스킨 보정 (staged pass, 2/2)

**보류 (2026-08-16):** `Correction1_Outfit/`과 함께 보류. 사용자 지시로 `../Test4/`(마스터
재생성)로 진행 중이니, Test4 결과가 나오고 이 체인이 다시 필요하다고 판단되기 전까지는 실행하지
말 것.

**주의 (2026-08-16 갱신 - 순서 변경됨, SUPERSEDED 아님):** `../Test3/
CLAUDE_QC_NEW_BATCH_UPDATED_2026-08-16.md`에서 Result 1/2/3 전체에 걸쳐 의상 좌우 비대칭 /
body-outfit blending이 HIGH priority 이슈로 새로 확인됨. Result 3도 예외가 아니라 "significant
outfit-fidelity failure"로 재평가됨 - 더 이상 "구조적으로 문제없고 스킨만 약한 후보"가 아닙니다.
이 패스를 Result 3 원본에 그대로 돌리면 깨진 의상 구조를 "바꾸지 말 것"으로 그대로 고정시키게
됩니다.

**적용 순서:** `Correction1_Outfit/`을 먼저 돌려 의상 구조부터 고칠 것 (대상도 Result 3이 아니라
UPDATED QC에서 새로 1순위가 된 Result 1). 이 스킨 보정 패스는 Correction1_Outfit의 출력 이미지를
입력으로 그 다음에 실행합니다.

## 왜 새 Test가 아니라 보정 패스인가

전략 문서 §2 규칙: "한 가지 QC 차원만 약하고 나머지는 다 괜찮다면, 프롬프트를 더 키우지 말고
보정 패스를 써라." Result 2의 반점/얼룩 아티팩트성 피부는 단일 시드 이상으로 진단됐고, 가슴
physics는 이 배치에서 이미 개선된 것으로 확인되어 **이번 보정 대상 아님** (UPDATED QC에서도
"Improved / Preserve"로 유지). 스킨 보정 패스와 의상 보정 패스는 §3의 경고("보정 두 개가
필요하면 합치지 말고 따로 두 번 돌려라 - 합치면 다시 다중 제약 희석 문제가 생김")에 따라 한
프롬프트로 합치지 않고 순서대로 두 번 돌립니다: 의상(구조적/HIGH) 먼저, 스킨(그 다음) 나중.

## 1. 입력

**`Correction1_Outfit/`의 출력 이미지 한 장만** 첨부하세요 (i2i, 다른 레퍼런스 불필요) - Result 3
원본이 아닙니다. `Correction1_Outfit/`을 아직 안 돌렸다면 이 패스보다 먼저 실행할 것.

## 2. 설정

| 항목 | 값 |
|---|---|
| 모드 | Image-to-image (텍스트만으로 새로 생성하지 말 것) |
| 모델 | Seedream 4.5 (Test3/Correction1_Outfit과 동일 - 한 번에 한 변수만 테스트) |
| 화면비 | 원본과 동일 (9:16) |
| 퀄리티 | high (Test3와 동일 유지 - quality 변경은 이 패스가 실패했을 때의 다음 단계) |

## 3. 프롬프트

`00_PROMPT.txt` 그대로 사용 (1036/3000자, 순수 ASCII, `prompt_check.py` PASS 확인 완료).

## 4. 확인할 것

- 피부가 자연스러운 포어/텍스처 쪽으로 개선됐는지, Result 2 같은 반점/얼룩/아티팩트가 새로
  생기지 않았는지
- 주근깨/점/여드름 등 원래 없던 피부 디테일이 추가되지 않았는지 (사용자가 명시적으로 금지한 부분)
- 얼굴/포즈/몸/의상(특히 Correction1_Outfit에서 고친 좌우 대칭)/배경/조명이 입력 이미지와 동일하게
  유지됐는지 (이 패스는 스킨 외엔 아무것도 안 바뀌어야 정상)

## 5. 실행 후

결과 이미지 + 짧은 판단을 이 폴더에 추가해주세요. 성공하면 이 보정된 이미지를 마스터로 승격
(promote)하는 걸 검토하고, `HIGGSFIELD_PROMPT_REVISION_STRATEGY_v1.md`에 이 보정 패스(2단계
체인 전체)의 첫 실제 적용 결과를 기록합니다. 실패하면 (아티팩트가 남아있거나 다른 부분이 바뀌면)
§3.1 템플릿 자체를 개선해야 할 신호이니 바로 다음 라운드로 넘기지 말고 원인부터 진단하세요.
